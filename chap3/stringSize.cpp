#include <iostream>
#include <string>

using std::cin; using std::cout;
using std::string;

int main()
{
    string line;
    getline(cin, line);
    int n = -1;

    while (line.size() < n);
    cout << "end";
}